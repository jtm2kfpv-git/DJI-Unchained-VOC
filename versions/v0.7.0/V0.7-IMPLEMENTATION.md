# DJI Unchained VOC v0.7 implementation matrix

This development snapshot starts from the preserved v0.6 source. The v0.6 folder and published release are not modified by v0.7 work.

Status meanings: **done** is implemented and automatically checked; **started** has an implemented foundation but still needs integration or device testing; **pending** has not yet been implemented.

| # | Agreed v0.7 change | Status |
|---:|---|---|
| 1 | Encode the DJI video directly instead of recording the whole screen | started — surface-fed hardware H.264/MP4 encoder added; GPU crop/render path pending |
| 2 | If MediaProjection remains temporarily, capture the app window by default | pending — migration code remains isolated |
| 3 | Use actual captured dimensions and resize callbacks | pending — superseded when direct pipeline lands |
| 4 | Validate portrait orientation for Shorts output | started — profile validation added |
| 5 | Offer 30, 50, and 60 fps | done in profile model; UI/encoder integration pending |
| 6 | Prevent raw and Shorts recorders from running together | done in coordinator model; activity integration pending |
| 7 | Lock aspect/display controls while recording | done in coordinator model; activity integration pending |
| 8 | Make the Shorts error state retryable | done in coordinator model; activity integration pending |
| 9 | Throttle per-frame UI work | started — encoder progress is limited to twice per second; decoder/UI integration pending |
| 10 | Check free space on the selected destination | done in the direct encoder using `fstatvfs` on the selected descriptor |
| 11 | Open output files with truncation semantics | done in the direct encoder with `rwt` |
| 12 | Remove incomplete MP4 output after a failed finalization | done in the direct encoder, subject to document-provider delete support |
| 13 | Add maximum recording size and duration limits | done in profile/limit model and enforced by the direct encoder |
| 14 | Add a reliable recorder status channel between background work and UI | started — coordinator snapshot/listener model added |
| 15 | Stop or warn when the app is hidden during legacy capture | pending |
| 16 | Provide a clean stop mechanism outside captured content | pending — direct pipeline removes the captured-control problem |
| 17 | Separate source aspect selection from the Shorts output mode | started — separate output-format model added; UI integration pending |
| 18 | Clearly distinguish 4:3 reinterpretation from cropping | pending |
| 19 | Label raw recording as the original incoming stream | pending |
| 20 | Make setup/health indicators time-aware | pending |
| 21 | Make the control panel responsive and scrollable | pending |
| 22 | Add adaptive and monochrome launcher icons | pending |
| 23 | Optimize large logo resources | pending |
| 24 | Refactor the oversized activity into focused components | started — recording/capture decisions moved into pure classes |
| 25 | Add Android instrumentation tests | pending |
| 26 | Run hardware regression tests | pending — requires Goggles N3/O4/Avata hardware |
| 27 | Decide final application/package identity | done — `local.n3view.voc`, with `.debug` for debug builds |
| 28 | Strengthen unofficial-project branding and warnings | started — development warning retained; final wording pending |
| 29 | Keep v0.7 explicitly experimental/pre-release | done in project documentation and release plan |

## Direct pipeline target

```text
DJI H.264 access units
        │
        ├──► raw H.264 recorder (original incoming stream)
        │
        └──► MediaCodec decoder → GPU crop/render
                                  ├──► phone preview
                                  └──► MediaCodec H.264 encoder → MediaMuxer MP4
```

This avoids recording application controls, removes the screen-capture consent path, and makes the 9:16 output independent of the phone display geometry.
